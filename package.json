{
  "name": "master-cat-skill-growth",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "gh-pages": "tsc && vite build --base /master-cat-skill-growth/ --outDir docs",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.4.3"
  }
}
